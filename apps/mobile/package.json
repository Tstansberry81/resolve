{
  "name": "resolve-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "RESOLVE — native iOS shell (Capacitor) around the hosted dashboard. Personal/dev use only.",
  "scripts": {
    "ios:add": "cap add ios",
    "sync": "cap sync",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
